// date - 1 july 2022    00:11
public class Abs {
    public static void main(String[] args) {
        int i, abs;

        i = -10;
        abs = i<0? -i : i;
        System.out.println("absolute value of " +  i + " is " + abs);

        i = 10;
        abs = i<0? -i : i;
        System.out.println("absolute value of " +  i + " is " + abs);

        // System.out.println( a < 0 ? -a : a);   //by rachit

        // int a;
        // Scanner sc = new Scanner(System.in);
        // System.out.println( (a = sc.nextant()) < 0 ? -a : a);    
        // created by rachit


        // chain assignment

        // int t,w,x;
        // t = w = x = 100;
        // System.out.println(t + w + x);

    }
}
